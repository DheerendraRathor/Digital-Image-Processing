function [U, S, V] = mySVD(X)
   [U, Uv] = eig(X * X');
   [V, Vv] = eig(X' * X);
   S = U' * X * V;
   
   [Sr, Sc] = size(S);
   for i = 1:min(Sr, Sc)
       if S(i
   end
   
   display(U);
   display(V);
   display(S);
   
   [U, S, V] = svd(X);
   
   display(U);
   display(V);
   display(S);
end